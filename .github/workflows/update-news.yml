name: Mise à jour des actualités

on:
  schedule:
    - cron: '0 6 * * *'  # Tous les jours à 6h du matin UTC
  workflow_dispatch:

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - name: Cloner le dépôt
        uses: actions/checkout@v3

      - name: Installer Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.x'

      - name: Installer les dépendances
        run: pip install requests

      - name: Générer headlines.json
        env:
          GNEWS_API_KEY: ${{ secrets.GNEWS_API_KEY }}
        run: python update_news.py

      - name: Commiter et pousser les changements
        run: |
          git config user.name "Robot Augusta"
          git config user.email "bot@radioaugusta.com"
          git add headlines.json
          git commit -m "📰 Mise à jour automatique des actualités" || echo "Aucun changement"
          git push
