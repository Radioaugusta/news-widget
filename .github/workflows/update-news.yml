name: Mise à jour des news

on:
  schedule:
    - cron: '0 7 * * *'  # Tous les jours à 7h UTC (9h Paris)
  workflow_dispatch:  # Lancement manuel possible

jobs:
  update:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout du dépôt
        uses: actions/checkout@v3

      - name: Configurer Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.x'

      - name: Installer les dépendances (si besoin)
        run: pip install requests

      - name: Lancer le script de mise à jour
        env:
          API_KEY: ${{ secrets.API_KEY }}
        run: python update_news.py

      - name: Authentification Git via le token personnel
        run: git remote set-url origin https://x-access-token:${{ secrets.GH_PAT }}@github.com/Radioaugusta/news-widget.git

      - name: Commit et push des titres mis à jour
        env:
          GH_PAT: ${{ secrets.GH_PAT }}
        run: |
          git config --global user.name "github-actions"
          git config --global user.email "github-actions@github.com"
          git add titres.json
          git commit -m "🔄 Mise à jour auto des titres - $(date +'%Y-%m-%d')" || echo "Aucun changement à committer"
          git push

  
