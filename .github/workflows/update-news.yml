name: Mise à jour des news

on:
  schedule:
    - cron: '0 7 * * *'  # Tous les jours à 7h UTC
  workflow_dispatch:

jobs:
  update:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout avec token personnel
        uses: actions/checkout@v3
  with:
    token: ${{ secrets.GH_PAT }}


      - name: Configurer Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.x'

      - name: Installer les dépendances
        run: pip install requests

      - name: Lancer le script de mise à jour
        env:
          API_KEY: ${{ secrets.API_KEY }}
        run: python update_news.py

      - name: Vérifier le contenu de titres.json
        run: cat titres.json

      - name: Authentifier Git avec token personnel
        env:
          GH_PAT: ${{ secrets.GH_PAT }}
        run: |
         

      - name: Commit et push des titres mis à jour
        run: |
          git config --global user.name "github-actions"
          git config --global user.email "github-actions@github.com"
          git add titres.json
          git commit -m "🔄 MAJ titres - $(date +'%Y-%m-%d')" || echo "⚠️ Aucun changement à committer"
          git push
